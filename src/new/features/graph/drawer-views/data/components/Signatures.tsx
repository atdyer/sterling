import React from 'react';
import SterlingDrawer from '../../../../../sterling/SterlingDrawer';

const Signatures: React.FunctionComponent = props => {

    return (
        <SterlingDrawer.Section
            collapsed={false}
            onToggle={() => {}}
            title={'Signatures and Sets'}/>
    );

};

export default Signatures;
