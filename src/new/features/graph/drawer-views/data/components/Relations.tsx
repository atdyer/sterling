import React from 'react';
import SterlingDrawer from '../../../../../sterling/SterlingDrawer';

const Relations: React.FunctionComponent = props => {

    return (
        <SterlingDrawer.Section
            collapsed={false}
            onToggle={() => {}}
            title={'Relations'}/>
    );

};

export default Relations;
